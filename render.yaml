services:
  - type: web
    name: samal-report-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -b 0.0.0.0:$PORT bot:app
    envVars:
      - key: BOT_TOKEN
        sync: false
